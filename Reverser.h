#include <string>

#pragma once

class Reverser {
    public:
    int reverseDigit(int value);
    std::string reverseString(std::string characters);

    int countValue(int value);
    int powerOfTen(int numOfDigits);
};